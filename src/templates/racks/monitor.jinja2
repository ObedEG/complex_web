{% extends "base.jinja2" %}
{% block content %}
    <div class="list-group ">
        <div class="row fullcables">
          <div class="col-lg-12">
            <div class="page-header">
              <h1 id="tables">RACKS UNDER TEST</h1>
            </div>

            <div class="bs-component">
                  <table class="table table-striped table-hover ">
                <thead>
                  <tr>
                    <th>Rack ID</th>
                    <th>Comments / Status</th>
                    <th>Rack Type</th>
                  </tr>
                </thead>
        {% for rack in racks %}
                 <!---class="list-group-item"> -->

                    <tr class="active">
                        <td><a href="{{ url_for('racks.monitor_rack', rack=rack._id) }}"><span class="glyphicon glyphicon-menu-right"></span>   {{ rack.rackid }}</td>
                        <td><strong>{{ rack.comments }}</strong></br></td>
                        <td>{{ rack.racktype }}</td>
                    </tr>

            <!--<a href="{{ url_for('racks.monitor_rack', rack=rack._id) }}" class="list-group-item">
                <h4 class="list-group-item-heading">
                    <span class="glyphicon glyphicon-menu-right"></span> Rack {{ rack.rackid }} || {{ rack.customer }}, lerom: {{ rack.lerom }}</h4>
                <p class="list-group-item-text"> Comments || Status: <strong>{{ rack.comments }}</strong></br></p>
                <p class="list-group-item-text"> Type: <strong>{{ rack.racktype }}</strong></p></a> -->
        {% endfor %}
                </tbody>
                </table>
            <div id="source-button" class="btn btn-primary btn-xs" style="display: none;">&lt; &gt;</div>
            </div><!-- /example -->
          </div>
        </div>
    </div>
{% endblock %}